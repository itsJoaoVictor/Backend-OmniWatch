from fastapi import APIRouter

router = APIRouter()

# Note: Media is currently used mainly as a local cache by the tracking services.
# Additional endpoints specifically for Media can be added here if needed.
